# Running quirks log

Things noticed mid-build that aren't bugs to fix now, but are worth remembering
before they bite. Append as we go; resolve and date when handled.

| # | Pass | Quirk | Status | Notes |
|---|------|-------|--------|-------|
| 1 | 3 | **Timezone in `dayKey`** — `timeline.ts` buckets days by slicing the *UTC* date (`instant.slice(0,10)`), not the user's local civil day. A late-night weigh-in can land on the wrong day; data-model principle 4 wants local days. | OPEN (acceptable) | Pass 3's *queries* use correct local-day bounds (`localDayWindow` in `lib/date.ts`). The trend engine's grouping is still UTC-based — fine in practice for a timezone behind UTC with morning weigh-ins (US Pacific/Mountain). Proper fix = make `dayKey`/`bucketByLocalDay` tz-aware; do it when sync/import lands or before shipping to other timezones. |
| 2 | 3 | **No `useSettings` hook existed** — game plan assumed one for units. | RESOLVED (2026-06-26) | Added `src/settings/useSettings.ts` stub, default `weightUnit: 'lb'` (Dylan logs in lb; storage stays kg via `lib/units.ts`). When settings get persisted, only this file changes. |
| 3 | 2,3 | **expo-sqlite doesn't run on web** — `getDb()` needs a wasm asset + metro tweak. Today now calls the DB, so a web load would throw on first query. | OPEN (deferred) | Phase-1 target is iOS sim / Expo Go. `getDb` dynamic-imports expo-sqlite, so importing the storage layer is safe; only *calling* it on web fails. Smoke-test on the simulator, not web. Revisit only if a web build is ever wanted. |
| 4 | 3 | **Refetch after modal save** — Today must reload when the Log Weigh-In modal closes. | RESOLVED (2026-06-26) | `useFocusEffect` on Today re-runs both hooks' `reload()` on focus. (Minor: hooks also self-load via `useEffect`, so first mount double-fetches — harmless.) |
| 5 | 3 | **Body-fat fidelity** — weight and body-fat % ride in one weigh-in Observation that carries a single `fidelity: 1.0`. Correct for scale weight, far too confident for scale body-fat (bioimpedance ≈ a guess without DEXA/calipers). Currently captured (`payload.bodyFatPct`, persisted + tested) but shown nowhere. | OPEN (design note) | Dylan's call, constitution-aligned. When body-fat is surfaced (Reflect / Phase 2), display it at LOW fidelity per brand-kit fidelity rules (dashed/hollow), never as a confident number, and never let it gate/contradict tier-1 data. Clean fix = per-field fidelity OR split body-fat into its own Observation with its own fidelity. Architecture call, not Phase-1. |
| — | 3→4 | **Pass 3 wasn't actually `tsc`-clean** — committed Pass 3 failed `npx tsc --noEmit` (2 test-only errors) despite the dev-log claiming clean; `jest` was green because it strips types. | RESOLVED (2026-06-26) | Fixed in commit `2951cdb` before Pass 4: `units.test.ts` now imports jest globals from `@jest/globals` (repo convention), and `weighInFromLb` is typed `ObservationOf<'weighIn'>` so the body-fat spread narrows. Lesson: `tsc` and `jest` are independent gates — run both. |
| 6 | 4 | **`reveal()` speaks engine-native units (kg, km), not the user's display unit.** A gym session's Today contribution reads `… 4,200 kg volume load` even though Dylan logs weight in lb; endurance reads `… 8.2 km`. | OPEN (deliberate) | The contribution line is the engine's voice, not a data-entry surface, so it stays unit-pure and deterministic (keeps `reveal` testable). `distanceUnit` defaults to `km` so endurance *entry* matches the *reveal* line for now. If it grates once real data is in, pass the display unit into `reveal(session, opts)` or convert in the UI — a Pass 5 call, alongside the ledger which has the same kg question. |
| 7 | 4 | **`computeWeeklyStimulus()` still throws** (`notImplemented`, "Pass 5"). | RESOLVED (2026-06-26) | Implemented for real in Pass 5: groups sessions by ISO week into per-pattern volume + per-energy-system minutes, warm-ups excluded (matches `reveal()`). Driven end-to-end by `src/__tests__/weeklyStimulus.test.ts`. The Reflect `StimulusLedger` consumes it via `useWeeklyStimulus`. |
| 8 | 4 | **Duration required on every session; no start/stop timer.** `SessionPayload.durationMin` is a non-optional number, so the form requires a duration even for a quick gym session. The spec's optional start/stop timer was deferred. | OPEN (acceptable) | One number, low friction. If logging friction shows up in the 2-week use test, either add the timer or make duration optional in the type (migration). Phase-1 keeps it simple. |
| 9 | 5 | **Climb/hike/other weeks underrepresented in the ledger bars.** Volume load is computed only from lifting working sets, so a week heavy on climbing or hiking shows short (or zero-height) bars even though real training happened. The sessions still appear in the week's drill-down (via `sessionIds`), just not in the bars. | OPEN (known, acceptable this pass) | Deliberate: the data model carries no measurable load for a climb send or a hike, so a bar for them would be a fabricated number (constitution: no fake data). Eventual fix = an energy-system parallel view (the engine already accumulates `byEnergySystem` minutes, currently unused by the UI) and/or richer climb data (grade-weighted load). Phase-1 keeps bars to weight-training volume and says so on screen ("bars show weight-training load only"). |
| 10 | 5 | **Weekly grouping buckets by UTC civil date**, via `isoWeekStart(dayKey(...))` — same UTC-slice basis as the trend engine (quirk 1). A late Sunday-night-local session whose UTC instant has rolled into Monday can land in the *next* ISO week. | OPEN (acceptable) | Same posture and same fix as quirk 1: fine for a timezone behind UTC with daytime logging (US Pacific/Mountain). Proper fix = tz-aware `dayKey`/week bucketing, done alongside quirk 1 when sync/import lands or before shipping to other timezones. The Reflect *window* (`useWeeklyStimulus`) derives its 8 Monday keys the same way, so engine and hook agree. |
| 11 | 2.1 | **Import-extension convention was stale.** CLAUDE.md + the run prompt called for `.js` suffixes on relative imports ("required for the tsx/Node ESM resolver"), but the actual tsconfig uses `moduleResolution: "bundler"` and **every existing `core/` file imports extensionless** (`from './observation'`). That's also what the green jest-expo setup resolves. | RESOLVED (2026-06-27) | New `core/src/nutrition/fidelity.ts` matched the real code (extensionless). Doc corrected: `planning/claude-md.md` Conventions now states imports are extensionless under bundler resolution and to *not* add `.js` suffixes. Code and doc now agree; don't churn the suffix. |
| 12 | 2.1 | **Tier boundaries now live in two places.** `core/src/nutrition/fidelity.ts` `tierOf` (HIGH/MID/LOW, 0.8/0.4) is the new canonical source, but `src/components/FidelityIndicator.tsx` still has its own `fidelityLevel()` with the same 0.8/0.4 cutoffs and lowercase `'high'/'mid'/'low'` levels. | OPEN (reconcile in 2.5) | Left untouched on purpose — 2.1 is core-only (no UI). Pass 2.5 builds the fidelity-tier visual and should make the component consume core's `tierOf` (mapping HIGH→high etc.) so the boundary numbers exist once. Until then they agree by coincidence, not by reference. |
| 13 | 2.2 | **OFF completeness signal is "fields present" only.** `openfoodfacts.ts` computes fidelity from how many of the four required macros a record carries, but does **not** yet cross-check self-consistency (do protein·4 + carbs·4 + fat·9 ≈ the stated energy?). The plan's wording was "present *and* self-consistent." | OPEN (refine later) | "Present" is the high-value half and is enough for the honest "missing → lower fidelity" behavior the spec needs. Self-consistency is a documented future refinement — add it as a second multiplier on the completeness signal when real OFF noise shows it's worth it. Tunable, no schema change. |
| 14 | 2.2 | **USDA Branded relies on `servingSize`.** A Branded food whose response lacks `servingSize` (or has it ≤ 0) yields **all-null macros** — the adapter does not fall back to the per-100g `foodNutrients` that Branded items usually also carry. | OPEN (acceptable) | Honest by default (null = not captured, never fabricated), but a recoverable record reads as empty. The captured fixture has `servingSize`, so the path is exercised; real Branded foods almost always include it. Fix when it bites = fall back to `per100gPerGram(raw)` for Branded-without-serving (the `branded` fidelity flag stays true regardless). |
| 15 | 2.3 | **`cached_foods` has no eviction / size cap.** Each lookup stores the raw source response (USDA detail blobs run tens of KB — cheddar is ~46 KB); `lastUsedAt` ranks recency but nothing prunes. | OPEN (acceptable single-user) | Fine at single-user scale (hundreds of foods = a few MB). Fix when it bites = an LRU prune (`DELETE … WHERE foodId NOT IN (SELECT … ORDER BY lastUsedAt DESC LIMIT N)`) or store normalized per-100g macros instead of the full raw blob. Raw was chosen so re-hydration reuses the 2.2 adapter exactly. |
| 16 | 2.3 | **USDA key defaults to `DEMO_KEY`** (api.data.gov public key, ~30 req/hr/IP). Search fires one request per (debounced) query, so a heavy session could throttle. | OPEN (wire before real use) | `foodSearch` already accepts `usdaApiKey` via deps. Pass 2.5 wires a registered **free** key (1000 req/hr) from app config (e.g. `expo-constants` `extra`). OFF needs no key. Still free-only — no paid tier (locked data-layer rule). |
| 17 | 2.4 | **`occurrencesFor` scans foodEntry rows via `json_extract(payload,'$.templateId')`** — no index on the JSON-embedded templateId, so it's a (filtered) table scan, and it relies on SQLite's JSON1 extension at runtime. | OPEN (acceptable) | JSON1 ships in expo-sqlite's bundled SQLite (≥3.38) and in better-sqlite3 (tests), so it's safe. Single-user food-log volume makes the scan negligible. Fix when it bites = a generated column `templateId AS (json_extract(payload,'$.templateId'))` + index, or denormalize templateId into its own observations column. |
